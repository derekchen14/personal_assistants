# DoE approval — round 5.1

## Orders echo
1. round_5.1_spec.md is authoritative; implement exactly; flag disagreements in notes, never deviate silently. 2. The mechanism is "NLU belief state injection" — never "mismatch gate"/"gate"/"mismatch anything"; belief is injected once per turn whether or not it matches. 3. Hook-point injection never blocks: _settle_nlu(wait=False), incorporate if landed, else continue; only Plan/Clarify's read_state blocks. 4. Flow differs + intent matches: orchestrator decides via prompt (defer to NLU 80%+). Intent differs: code forces it (Active -> Pending, stackon pred_flows[0]). Never swap these. 5. Policy-execution failures re-consult nlu.understand(op='contemplate'), never op='think'. 6. Plain language: no seam/envelope/steward/stewardship/hydrate (rehydrate_flow identifier exempt)/sentinel/byte-identical/genuinely/honestly/load-bearing/delve/tighten anywhere. 7. Output compact, under 1500 words total. 8. CLAUDE.md rules: simplicity first, surgical changes, no defensive programming, trimmed param style, 100-char lines, no new concepts beyond the spec's two.

## Verdict
SWE1 wins. SWE2's plan is a placeholder ("test" in every field) — rejected outright, nothing to adjudicate. SWE1's plan matches the spec's build list item for item, every line anchor checks out against current code, and it obeys all 8 orders (injection named and built per order 2, wait=False non-blocking per order 3, prompt-vs-code split per order 4, contemplate per order 5). Its one interpretive call — force only on a domain pred_intent (Research/Draft/Revise/Publish), note-only for Plan/Clarify/Converse differences — is exactly the spec's Big decision 3 condition ("a domain pred_intent"), not a deviation. Approved with 4 DoE corrections from the ponytail pass: (a) no 6th PEX.__init__ param — wire nlu by attribute after construction (CLAUDE.md 5-param cap); (b) mirror the forced pause onto the live flow stack — activate_flow line 660 resyncs state.flow_stack FROM the live stack, so a state-file-only pause would be clobbered back to Active; (c) shrink the pex.py:578 key loop once plan_id is gone; (d) drop the duplicated pred_flows check inside the force branch. Suite-file fixes and tests stay with the orchestrator per spec.

## Ponytail (line-item)
- pex.py:L578: shrink the 2-key loop; with plan_id gone a 1-tuple loop is noise. Replace with: if 'flow_name' in params: kwargs['flow_name'] = params['flow_name'].
- SWE1 _inject_belief (new code): delete the second 'pred_flows non-empty' check inside the intent-differs branch; the early return already guarantees it.
- SWE1 pex.py __init__ + agent.py:L44: yagni a 6th constructor param (breaks the 5-param cap). After construction in agent.py: self.pex.nlu = self.nlu.
- dialogue_state.py:L215: delete plan_id=kwargs.get('plan_id'); call becomes stack.stackon(kwargs['flow_name']). Already in plan — confirmed correct.
- revise.py:L67-69, L227-231: delete both has_plan scratchpad blocks whole; completion records already carry the summary, no replacement needed.
- parents.py:L16: pre-existing comment mentions Plan flows keeping Pending status; leave it (surgical rule) — it describes Pending, which survives.
- backend/prompts/pex/skills/plan.md: keep under 20 lines; reuse load_skill_template (components/prompt_engineer.py:695) — no new loader.
- net: -30 lines possible.

## Approved direction
Build SWE1's plan with 4 corrections. Branch round/5.0-pex-hooks. Repo root /Users/derekchen/Documents/repos/personal_assistants, code under assistants/Hugo. Obey the 8 orders in orders_echo above verbatim — they are the user's non-negotiables; re-read them before coding.

1. NEW backend/prompts/pex/skills/plan.md (<20 lines) — the Workflow Planner skill, guidance only, returns nothing: read belief and the flow catalog; map each sub-task to an EXISTING catalog flow (never invent one); order by dependency, keep it minimal; stage and run ONE AT A TIME (stackon the next only after the current completes — stacking the whole sequence up front breaks the single-Active model); share a one-line plan with the user; after each flow judge whether the user's goal is met, then stage the next or conclude and report what was done.

2. backend/prompts/for_orchestrator.py — in build_orchestrator_prompt parts (after outline_levels): f'<workflow_planner>\n{engineer.load_skill_template("plan")}\n</workflow_planner>'. In TOOL_POLICY: point the existing Plan bullet at that guidance, and add a belief-note rule: when a [belief] note names a different flow than the one you are on, defer to NLU's detection unless you have a concrete reason to stay — defer in 80%+ of cases.

3. backend/modules/pex.py:
- execute(): set self._injected = False beside self._completed_this_turn.
- New _inject_belief() -> str|None: self._settle_nlu(wait=False); state = self.world.current_state(); return None if self._injected, or self._nlu_thread is not None (detection not landed), or not state.pred_flows. Else self._injected = True; build the note using the spec's exact wording: "[belief] this turn's detection — intent: X, flow: f (0.86), slots: {...}. If you are on a different flow, prefer NLU's detection unless you have a concrete reason to stay." Then the intent-differs force IN CODE: active = self.flow_stack.get_flow(status='Active'); if active and not self.ambiguity.present() and state.pred_intent in ('Research','Draft','Revise','Publish') and state.pred_intent != active.intent: state.write_state(self.world.state_file(), 'update_flow', status='Pending'); state.write_state(self.world.state_file(), 'stackon', flow_name=state.pred_flows[0]['flow_name']); self._apply_belief_slots(state, that flow_name); ALSO set active.status = 'Pending' on the live stack (activate_flow:660 resyncs state.flow_stack from the live stack — without the mirror the pause is clobbered back to Active); append a sentence to the note stating what was forced. Flow-differs-only gets NO code action — the note plus the prompt rule decide (order 4).
- Carrier A (hooks 2/3/4): in _run_loop after the results loop (line ~385), note = self._inject_belief(); if note: results.append({'type':'text','text':note}) — before context.append_message(results).
- Carrier B (hook 5): in the `if not tool_uses:` / `if text:` branch, note = self._inject_belief(); if note: append the assistant text message, append {'role':'user','content':note}, and continue one more round instead of returning; else return text as today.
- Contemplate route (order 5): in the post-tool loop, when tool_use.name == 'activate_flow' and result.get('_error') == 'execution_error': self.nlu.understand(op='contemplate', user_text=self.world.context.last_user_text); self._injected = False so the fresh detection is re-injected. Never op='think'.
- nlu wiring: do NOT add a 6th __init__ param. In agent.py after line 44's PEX construction add: self.pex.nlu = self.nlu.
- plan_id removal: replace the :578-580 loop with `if 'flow_name' in params: kwargs['flow_name'] = params['flow_name']`; delete the plan_id schema property (:979) and "(optional `plan_id`)" in the write_state description (:960).

4. shared/shared_defaults.yaml:55 — max_flow_depth: 16, comment: plans stack several sub-flows. flow_stack/stack.py:12 — fallback get('max_flow_depth', 16). Keep the RuntimeError (:98-101).

5. Removals. stack.py: stackon(:17) drops plan_id param, _push call (:27) passes only flow_name, _push(:97) drops the param and the flow.plan_id line (:108). parents.py: delete :14 plan_id attr and the :87 to_dict entry; leave the :16 comment. dialogue_state.py: delete plan_id from rehydrate_flow (:44), the write_state docstring (:170), and _run_stack_op (:215 becomes stack.stackon(kwargs['flow_name'])); delete has_plan from _BELIEF_FIELDS (:9), __init__ (:62), reset (:100), serialize (:115), serialize_session docstring (:122) and flags dict (:129), load (:155), from_dict (:249). Keep has_issues. revise.py: delete the has_plan blocks at :67-69 and :227-231 whole.

6. Do NOT write tests or touch utils/ suite files (nlu_unit_tests, mem_unit_tests, _snapshot) — orchestrator's job per spec. Verify by running the free suite locally to confirm only the known suite-file failures remain.

Known accepted risks (from SWE1, per order 1): the force mutates state mid-loop like prestage does pre-loop — the live-stack Pending mirror above closes the resync gap; step_5 §5.4's claim that the flags block ends empty is wrong while has_issues survives — follow the round spec's anchors.
