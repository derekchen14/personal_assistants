# DoE adjudication — round 5.1

## Orders echo
1. round_5.1_spec.md authoritative, implemented exactly; disagreements go in notes. 2. Mechanism is "NLU belief state injection", never gate/mismatch; injected once per turn regardless of match. 3. Hook injections never block — _settle_nlu(wait=False) only; blocking join exists solely in Plan/Clarify's read_state. 4. Flow-differs+intent-matches: orchestrator prompt decides (defer to NLU 80%+); intent-differs: code forces Active→Pending + stackon pred_flows[0]. 5. Policy-execution failures re-consult nlu.understand(op='contemplate'), never 'think'. 6. Plain language, banned-word list respected. 7. Output compact, under budget where the verbatim diff allows. 8. CLAUDE.md: simplicity, surgical, no defensive code, trimmed params, 100-char lines, no concepts beyond the spec's two.

## Winner
SWE1 — shipped verbatim (no composition needed).

## Divergence
6 of 9 files are identical between builds (dialogue_state, parents, stack, revise, write_state schema removals; agent.py differs only in comment text). Real differences, all in pex.py / for_orchestrator.py / plan.md / yaml: (a) SWE1 initializes _injected and nlu in PEX.__init__, matching the existing _nlu_thread/_completed_this_turn pattern; SWE2 leaves both undeclared until runtime wiring. (b) _inject_belief: SWE1 fetches state once, uses the per-flow confidence pred_flows[0]['confidence'] (matches the spec's "rework (0.86)" example) and json.dumps for slots; SWE2 fetches state twice, uses the overall state.confidence, and emits Python-repr slots (single quotes, inconsistent with the JSON elsewhere in context). (c) Carrier B: SWE1 injects after appending the assistant text, SWE2 before — same resulting message order, a wash. (d) Contemplate check before (SWE1) vs after (SWE2) results.append — equivalent. (e) Belief-notes prompt rule: SWE1 anchors it beside the Plan bullet and names the <workflow_planner> tag; SWE2 parks it after Publishing with "in your prompt". (f) yaml: SWE1 keeps the knob's meaning in the comment; SWE2's comment drops it. Also decisive: SWE1's worktree sits on the correct base (08e6ecd) and its diff applied cleanly; SWE2's worktree was on fa49a89 with the entire round-5.0 file set staged, so its 9-file diff had to be reconstructed against 08e6ecd — recoverable, but SWE1 needed no reconstruction.

## Order compliance
Order-by-order for the shipped (SWE1) diff: (1) PASS — build-list items 1-5 implemented per spec anchors; item 6 (tests) excluded per direction. (2) PASS — grep over the diff: no "gate"/"mismatch"; the only "gate" hit is pre-existing "delegate_task" context. Injection fires once per turn whether or not detection matches. (3) PASS — _inject_belief calls _settle_nlu(wait=False) and skips while _nlu_thread is alive; no new join added; the only blocking join remains _dispatch_read_state. (4) PASS — code force runs only when pred_intent differs from active.intent (update_flow status=Pending, stackon pred_flows[0], _apply_belief_slots, live-stack mirror); flow-differs gets prompt guidance only. (5) PASS — nlu.understand(op='contemplate', user_text=last_user_text) on activate_flow execution_error; understand() routes op=='contemplate' to contemplate() (nlu.py:93,118); no 'think' call. (6) PASS — banned-word grep clean; only exempt rehydrate_flow/_rehydrate_stack identifiers and pre-existing context lines hit. (7) PASS on my fields; the verbatim diff alone is 2269 words — conflict noted in notes. (8) PASS — 9 files, 82+/36-, no defensive guards, no new concepts beyond the skill file and the injection state; two lines run 101-104 chars, under the 120 hard stop. Correctness verified: injection exactly once across both carriers via the shared _injected flag (contemplate's reset is direction-approved re-arm); carrier A's text block rides AFTER the tool_result blocks in the same user message (API-legal); carrier B's free-standing user note follows a plain assistant text message, never splitting a tool_use/tool_result pair; grep confirms zero plan_id/has_plan references remain in backend/ or schemas/ after the diff; rehydrate_flow no longer reads a key to_dict no longer writes. Free suite in SWE1's worktree: 211 passed, 4 failed — exactly the suite-file failures reserved for the orchestrator (3 nlu_unit_tests asserting plan_id/has_plan, 1 pex test_skill_tools_match_flow flagging the new guidance-only plan.md).

## Ponytail (line-item)
- pex.py __init__ self.nlu=None + self._injected=False (SWE1 only, +2 lines): KEEP — attributes exist at construction, same pattern as _nlu_thread/_completed_this_turn; SWE2's omission saves 2 lines but leaves nlu created from outside the class only.
- _inject_belief single current_state() fetch (SWE1) vs double fetch (SWE2): KEEP SWE1 — one call, one variable.
- Per-flow confidence + json.dumps slots (SWE1) vs overall confidence + Python repr (SWE2): KEEP SWE1 — matches the spec's example note and the JSON everywhere else in context; zero extra lines.
- Carrier B inject-before vs inject-after assistant append: identical message order — wash, no change.
- Contemplate check placement before vs after results.append: equivalent — wash.
- Belief-notes rule beside the Plan bullet naming <workflow_planner> (SWE1) vs after Publishing (SWE2): KEEP SWE1 — the rule sits where Plan behavior is defined.
- yaml comment keeping 'max flows on stack simultaneously' (SWE1): KEEP — the comment still says what the knob is, not just why it changed.
- Deletion scan: nothing speculative found in SWE1 to cut — no unused params, no extra guards, no just-in-case fields; the two spec'd concepts are the only additions.
- Net: SWE1 wins 4 line items, SWE2 wins 0 (its 2-line-shorter __init__ trades away the established attribute pattern). Ship SWE1 unchanged.

## Notes
Ship SWE1 verbatim; the exact diff is also saved at /private/tmp/claude-501/-Users-derekchen-Documents-repos-personal-assistants/d8a47d03-22bc-4a83-ae1d-61010c02f371/scratchpad/round_5.1_final.diff (extracted from SWE1's worktree vs 08e6ecd, the round/5.0-pex-hooks tip). Order conflict flagged per order 1: the diff alone is 2269 words, so the verbatim-diff requirement makes the 1500-word total budget of order 7 impossible; every other field is kept short. Verified contracts: pred_flows entries carry per-flow 'confidence' (nlu.py:525), update_flow accepts status kwarg with grounding check only on Completed, understand() routes op='contemplate' (nlu.py:93). Free suite in SWE1's worktree: 211 passed / 4 failed — the 3 nlu_unit_tests plan_id/has_plan assertions plus pex test_skill_tools_match_flow flagging plan.md as an orphan skill (add 'plan' to its non-flow allowlist); all four are suite-file fixes the orchestrator owns per the spec. Accepted risks carried forward: the intent-differs force mutates state mid-loop (live-stack Pending mirror closes the activate_flow:660 resync gap); step_5 §5.4's claim that the flags block ends empty is wrong while has_issues survives. Minor pre-existing: _guarded_call's docstring contains a banned word — untouched per surgical-changes rule; two added lines run 101-104 chars (under the 120 hard stop).

> Post-adjudication (orchestrator, per Derek): renames _dispatch_read_state -> read_state,
> _settle_nlu -> _check_nlu, _inject_belief -> inject_belief_state; suite-file fixes; tests.
