# SWE1 implementation plan — round 5.1

## Orders echo
1. Spec is authoritative; implement exactly, flag concerns in risks.
2. Term is "NLU belief state injection"; injected every turn, match or not.
3. Hook injections never block: _settle_nlu(wait=False); only Plan/Clarify block.
4. Flow-differs+intent-match=prompt decides; intent-differs=code forces pause+stackon.
5. Policy-execution failures re-consult op='contemplate', never op='think'.
6. Plain language: no seam/envelope/steward/hydrate/sentinel/etc.
7. Output compact, under 1500 words.
8. Simplicity, surgical, no defensive code, no new concepts beyond the two.

## Summary
Plan-only (no edits). Two new concepts per spec: (1) the Workflow Planner skill file plan.md read into Tier 2 via load_skill_template; (2) belief-state injection = a per-turn `_injected` flag + one `_inject_belief()` builder with two carriers in `_run_loop`. Plus depth 8-16 and the has_plan/plan_id removals at the spec's anchors. PEX gains an `nlu` reference (wiring, not a new concept) so policy-execution failures can re-consult op='contemplate'. Tests are the orchestrator's job (utils/ off limits). Suite-file fixes for the removals (nlu_unit_tests/mem_unit_tests/_snapshot) are also the orchestrator's, per spec.

## Changes

- **backend/prompts/pex/skills/plan.md**: NEW file, <20 lines, the Workflow Planner skill (how-to, returns nothing). Body: read belief and the flow catalog; map each sub-task to an EXISTING catalog flow (never invent one); order by dependency; stage and run ONE AT A TIME (stackon the next only after the current completes — do NOT stack the whole sequence up front, it breaks the single-Active model); share a one-line plan with the user; after each flow judge whether the user's goal is met, then stage the next or conclude and report what was done. Resolves via engineer.load_skill_template('plan') (pex/skills/ is _SKILL_DIRS[0]).
- **backend/prompts/for_orchestrator.py**: build_orchestrator_prompt: add a Tier-2 block `<workflow_planner>\n{engineer.load_skill_template('plan')}\n</workflow_planner>` in the parts list (after outline_levels). In TOOL_POLICY: point the existing Plan bullet at that guidance, and add a short belief-note rule: when a `[belief]` note names a different flow than the one you're on, defer to NLU's detection unless you have a concrete reason to stay (defer 80%+). No byte-stability concern: skill body is read once at session build.
- **backend/modules/pex.py**: execute(): reset `self._injected=False` beside `_completed_this_turn`. Add `_inject_belief()`: `_settle_nlu(wait=False)`; return None if `self._injected`, if `self._nlu_thread is not None` (still running), or if no `state.pred_flows`; else set `_injected=True`, format the note, and if `state.pred_intent` differs from the Active flow's intent (active = flow_stack.get_flow(status='Active'); guard: active exists, no ambiguity.present(), pred_intent in the 4 domain intents, pred_flows non-empty) force it: `state.write_state(update_flow, status='Pending')` then `state.write_state(stackon, flow_name=pred_flows[0])` then `_apply_belief_slots` (mirrors prestage), and append the forced-switch line to the note; return note. Note format: `[belief] this turn's detection - intent: X, flow: f (0.86), slots: {...}. If you are on a different flow, prefer NLU's detection unless you have a concrete reason to stay.` Carrier A (tool round, hooks 2/3/4): in `_run_loop` after building `results`, `note=self._inject_belief(); if note: results.append({'type':'text','text':note})` before append_message. Carrier B (hook 5, text-only): in the `if not tool_uses: if text:` branch, before returning, `note=self._inject_belief(); if note:` append the assistant text, then append `{'role':'user','content':note}`, and `continue` one more round instead of returning. Contemplate route: in the post-tool loop, when `tool_use.name=='activate_flow'` and `result.get('_error')=='execution_error'`, call `self.nlu.understand(op='contemplate', user_text=self.world.context.last_user_text)` and set `self._injected=False` so the re-route re-injects. Add `nlu` param to `__init__` (store `self.nlu=nlu`). Remove `plan_id` from `_dispatch_write_state` line 578 tuple -> `('flow_name',)`, and drop the `plan_id` schema property + its mention in the write_state description (~lines 962, 979).
- **backend/agent.py**: Line 44: pass nlu into PEX -> `self.pex = PEX(self.config, self.ambiguity, self.engineer, self.memory, self.world, self.nlu)`. nlu is already built on line 43, so ordering is fine.
- **shared/shared_defaults.yaml**: Line 55: `max_flow_depth: 16` (was 8); update the comment to note plans stack several sub-flows.
- **backend/components/flow_stack/stack.py**: Line 12: default fallback `get('max_flow_depth', 16)`. Keep the overflow RuntimeError (:98-101). Remove plan_id: stackon signature (:17) drops `plan_id` param and the `_push(flow_name, plan_id)` call (:27); `_push` signature (:97) drops it and the `flow.plan_id = plan_id` line (:108).
- **backend/components/flow_stack/parents.py**: Remove `self.plan_id: str | None = None` (:14) and the `'plan_id': self.plan_id` entry in to_dict (:87). Leave the unrelated pre-existing comment at :16.
- **backend/components/dialogue_state.py**: Remove has_plan: from _BELIEF_FIELDS (:9), __init__ (:62), reset (:100), serialize (:115), serialize_session flags dict (:129), load (:155), from_dict (:249), and its mention in the serialize_session docstring (:122). Keep has_issues (out of this round's scope). Remove plan_id: `flow.plan_id = entry['plan_id']` in rehydrate_flow (:44), the `plan_id=kwargs.get('plan_id')` arg in _run_stack_op stackon (:215), and the plan_id mention in the write_state docstring (:170).
- **backend/modules/policies/revise.py**: Delete the `if state.has_plan:` scratchpad-write block in rework_policy (:67-69) and the one in write_policy (:227-231) — completion records already carry the summary, so these are redundant.

## Risks
Intent-differs forcing mutates via state.write_state (mirrors prestage), which updates state.flow_stack; the live self.flow_stack may briefly diverge until activate_flow's epilogue resyncs. Same pattern prestage already relies on; low risk but the injection runs mid-loop rather than pre-loop. I gate the force on a domain pred_intent with a non-empty pred_flows and an existing Active flow; Plan/Clarify/Converse intent-differences get noted but not force-staged (they lack a concrete pred_flows[0] to stack) — a mild reading of spec order 4, flagged here per order 1. step_5 §5.4 claims the flags block ends empty, but has_issues stays (not in this round's anchors) so the block keeps has_issues; I follow the round_5.1 anchors, not that aside. Carrier A folds hooks 2/3/4 into one post-tool insertion point (matches big-decision A) rather than three literal call sites. PEX now needs an nlu handle for contemplate — pure wiring, not a new concept. Suite files still reference the removed fields; per spec the orchestrator fixes those (utils/ off limits to me), so the free suite will be red until it does.

Line estimate: 95
