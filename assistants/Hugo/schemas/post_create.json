{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "post_create",
  "description": "Create a new draft post with title and optional initial content",
  "type": "object",
  "properties": {
    "title": {
      "type": "string",
      "description": "Title of the new post"
    },
    "topic": {
      "type": "string",
      "description": "Topic or theme for the post"
    },
    "type": {
      "type": "string",
      "enum": ["draft", "note"],
      "description": "Type of content to create: 'draft' for a full blog post, 'note' for a shorter snippet"
    },
    "outline": {
      "type": "object",
      "description": "Initial outline structure (sections with key points)"
    }
  },
  "required": ["title"],
  "additionalProperties": false
}
