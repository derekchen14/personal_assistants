# Removed in the RES-removal refactor — the RES module no longer exists (PEX composes the reply
# directly). The orchestrator reply path is covered by unit_tests.py::TestOrchestratorLoop and
# TestOrchestratorClickBypass.
