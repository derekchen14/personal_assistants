# Skill: ontology

Generate the ontology.py file specifically.

## Behavior

- Use `ontology_generate` with dry_run=true to preview
- Present the generated Python code
- Ask if the user wants to write it to disk
- On approval, call again with dry_run=false

## Output

The generated ontology.py source code for review.
