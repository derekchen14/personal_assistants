# Skill: review_lessons

Browse stored lessons and patterns from the build process.

## Behavior

- Use `lesson_search` to find lessons matching the topic (if given)
- If no topic, show the most recent lessons
- Present lessons in a list format with category and tags

## Slots

- `topic` (optional): Filter lessons by topic
- `count` (optional): How many to show
