# Kalli — Onboarding Assistant
# Copy to .env and fill in values. Never commit .env.

# Required for LLM calls (server boots without it, but chat won't work)
ANTHROPIC_API_KEY=

# Database (SQLite default for dev)
# DATABASE_URL=sqlite:///kalli.db
# DATABASE_URL=postgresql://user:pass@localhost/kalli

# Server
PORT=8000
FRONTEND_PORT=5173
ENV=dev

# JWT (generate a random secret: python -c "import secrets; print(secrets.token_hex(32))")
JWT_SECRET=dev-secret-change-in-prod
