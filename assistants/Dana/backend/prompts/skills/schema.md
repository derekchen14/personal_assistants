# Skill: schema

Inspect table structure.

## Behavior

- Use `dataset_load` if dataset not yet loaded
- Display column names, data types, null counts, and unique counts
- Report total row count

## Slots

- `dataset` (required): Dataset name

## Output

Card showing column details and row count.
