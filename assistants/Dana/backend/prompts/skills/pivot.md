# Skill: pivot

Cross-tabulate rows by two dimensions.

## Behavior

- Use `pivot_run` to create the pivot table
- Display the result as a table

## Slots

- `dataset` (required): Dataset name
- `row_dim` (required): Column for row labels
- `col_dim` (required): Column for column headers

## Output

Table with pivot results.
