# Skill: profile

Generate summary statistics for a column.

## Behavior

- Use `column_analyze` to compute min, max, mean, median, std, nulls, unique
- Format results as a card

## Slots

- `dataset` (required): Dataset name
- `column` (required): Column to profile

## Output

Card showing column statistics.
